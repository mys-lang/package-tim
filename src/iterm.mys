from base64 import encode

def write(image: bytes):
    """Write given image to the terminal.

    """

    print(f"\x1b]1337;File=size={len(image)};inline=1:", end="")
    print(encode(image), end="")
    print('\a', end="")
